# IMAGE_DIGEST — base-image pin record (Step 0.1.1, roadmap §1.3)
# One-time pull: 2026-06-11, podman 5.8.2 (rootless), host Fedora 44.
# Command: podman pull docker.io/library/ubuntu:24.04

# PIN USED IN Containerfile FROM (amd64 single-image OCI manifest digest — strictest pin:
# freezes the exact manifest bytes -> exact config + layer set. Chosen over the index
# digest because the project runs on one pinned amd64 host; multi-arch resolution is
# deliberately excluded. Delegated design choice recorded per §1.3.)
PINNED_MANIFEST_DIGEST=sha256:023f8a753c22258c9fe2d0005a7d28258038da7d620e9f93e9ad78aa266f9f11

# Multi-arch OCI image index (manifest list) digest of ubuntu:24.04 at pull time.
# Evidence: podman manifest inspect of this digest returned
# mediaType application/vnd.oci.image.index.v1+json whose amd64/linux entry is exactly
# PINNED_MANIFEST_DIGEST above.
INDEX_DIGEST=sha256:786a8b558f7be160c6c8c4a54f9a57274f3b4fb1491cf65146521ae77ff1dc54

# Image ID (= digest of the OCI image config referenced by the pinned manifest).
IMAGE_ID=sha256:8bf6fbc94074daccf0d7e43395600ddd71aa5a9e486cf0d1a19cc36583f8401e

# Layer set of the pinned manifest.
LAYER_BLOB_TARGZ=sha256:cb259a83ac3dd9fea0b394df41df2b298adf0df938fef5999475af18a751c257
ROOTFS_DIFF_ID=sha256:98effb2dfe85d4c431f97d90482075f19e5fc3a57c2dd423d8bdfd4813620043

# Upstream image creation time (from image config): 2026-05-20T01:37:22Z
# Bit-identity rebuild evidence: /logs/env/STEP_0.1.1_build_identity.log
