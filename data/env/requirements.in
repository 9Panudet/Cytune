# Motif+BO top-level Python dependencies (Step 0.1.3, roadmap §1.3).
# Resolved INSIDE the pinned image (python3.12) by pip-compile --generate-hashes
# --allow-unsafe -> requirements.lock (the artifact that pins EVERYTHING, by hash).
# Exact resolver invocation + version: logs/env/STEP_0.1.3_python_pins.log.
Cython>=3,<4
smac>=2,<3
# CARRIED CAVEAT (defect D3, /logs/defects/D3.md): smac 2.4.0 imports the private
# sklearn.tree._tree.DTYPE, removed in scikit-learn 1.9.0, and declares no upper bound
# (scikit-learn>=1.6.1). Without this cap the resolved env fails at `import smac`.
# Revisit only if smac >2.4.0 fixes the import.
scikit-learn<1.9
numpy
pytest
# Property-based testing library. Added at the 0.4.2->0.4.3 boundary on explicit
# user request (roadmap §3.3 names Hypothesis for the property-based validation
# cases). Capped to the current major (6.x) for re-pin stability. Re-pinned the
# WHOLE lock in-image with pip-tools 7.5.3 (the Step-0.1.3 resolver) with the prior
# lock present so existing versions were PRESERVED — only hypothesis + its deps
# (attrs, sortedcontainers) were added; no cascade bump (verified by lock diff).
# VALIDATION before commit: rebuilt the image from the new --require-hashes lock,
# scripts/check_python_env.sh clean (hash match, `import smac` D3 guard, RF backend
# guard), full pytest suite 211/211 green. Evidence:
# logs/env/STEP_0.4.2b_hypothesis_pin.log. Rollback condition (suite break / cascade)
# did not trigger. NOTE: Phase-0 reference-kernel property cases still use the
# committed fixed-seed numpy corpus; Hypothesis generators are wired at the corpus
# Steps 1.1.3/1.1.5 where input manifests are frozen.
hypothesis>=6,<7
