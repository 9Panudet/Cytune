# STEP 0.4.2 — known-good build runs the full validation input set CLEAN (§3.3, Phase-0 exit evidence)

Grounding: roadmap §3.3 (ASan+UBSan clean on the validation set; set is diverse by
construction — golden + edge + property-based), §0.5.2 (known-good = all checks off,
cdivision on), §3.2(2) (fresh subprocess per case).

DEVIATION (stated, not silent): Hypothesis is not in the pinned lock. Property-based
cases use a fixed-seed numpy corpus (deterministic, committed) instead of the Hypothesis
library. The library decision is deferred to corpus Steps 1.1.3/1.1.5. Same substance
(randomised valid inputs, fixed seed, committed) with no dependency change at Phase 0.

Authoritative result: results/sanitize/known_good/{REPORT.md,known_good_sanitize.json}
  -> 75 cases (csr 37 + pava 38), 0 sanitizer reports, CLEAN.

## NON-VACUITY (two axes)
1. Input validity: src/tests/test_validation_inputs.py asserts every materialised case
   honours its kernel's memory-safety contract (indptr monotone & == len(data),
   array lengths == n, dtypes, contiguity). A report would be a real kernel defect,
   never a malformed input. (77 contract tests; in full-suite 211 passed.)
2. Detection path: NEGATIVE CONTROL — csr_scale with an injected OOB write
   (data[j+999999]) built with the SAME known-good directives + sanitizer profile and
   run through the SAME _san_child.py + gate env is FLAGGED (child rc=1, ASan report).

=== negative control output ===
child returncode: 1  (expect nonzero)
DETECTED: sanitizer report present

=== full suite ===
211 passed (incl. 77 validation-input contract tests)
