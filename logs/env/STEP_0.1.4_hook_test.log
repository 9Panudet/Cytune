# Step 0.1.4 evidence — scaffold, git init, loose-root-file pre-commit hook
# Date: 2026-06-11 (UTC).

## 1. Scaffold (roadmap §1.4)
data/ logs/ results/ pre-existed (Steps 0.1.1–0.1.3 artifacts); created src/ and
sandbox/ with .gitkeep. sandbox/* gitignored (per-candidate scratch, wiped, never
versioned; .gitkeep retained).

## 2. git init
git init -b main; repo-local identity user.name=beimfedora,
user.email=panudetsuttiwong48@gmail.com.

## 3. Hook
Source of truth: scripts/hooks/pre-commit (committed); copy-installed to
.git/hooks/pre-commit. Deliberately NOT core.hooksPath, so graphify hook install
(Step 0.1.7) can coexist in .git/hooks/. Policy: staged depth-0 paths must match the
explicit allowlist (CLAUDE.md, Motif+BO-Roadmap.md, Containerfile, .containerignore,
.gitignore); nested paths unrestricted.

## 4. Deliberate-violation test (the Step's check)
$ echo "deliberate violation" > LOOSE_TEST.txt && git add -A && git commit -m "test: ..."
  pre-commit: REJECTED loose root file: 'LOOSE_TEST.txt' (allowlist: scripts/hooks/pre-commit)
  commit_exit=1        <- commit blocked, as required
Violation removed (git rm --cached + rm).

## 5. Positive control
The initial commit immediately following this log (all artifacts, all nested + 5
allowlisted root files) must succeed with the same hook active — its existence in
`git log` is the positive-control evidence. Commit hash recorded in STATE at the
0.1.4 boundary.

RESULT: PASS — hook blocks loose root files (negative test) and admits the
allowlisted/nested set (positive control = initial commit).
